# .gitignore 快速参考

## 🚀 快速清理已跟踪的开发文档

如果你的开发文档已经被Git跟踪，运行以下命令清理：

```bash
# 1. 移除 .kiro 目录
git rm -r --cached .kiro/

# 2. 移除开发文档
git rm --cached ScanIDCard_full_extract.md

# 3. 提交更改
git commit -m "chore: remove development documentation from git tracking"

# 4. 推送到远程（如果需要）
git push
```

## 📋 常用命令

### 查看被忽略的文件
```bash
git status --ignored
```

### 检查特定文件是否被忽略
```bash
git check-ignore -v filename.md
```

### 查看将被清理的文件（预览）
```bash
git clean -ndX
```

### 实际清理被忽略的文件
```bash
git clean -fdX
```

## 🎯 现在被忽略的内容

### 开发文档
- `.kiro/` - 所有Kiro AI规范
- `*_extract.md`, `*_notes.md`, `*_plan.md` 等
- `TODO.md`, `NOTES.md` 等临时文件

### 临时文件
- `*.bak`, `*.tmp`, `*.swp` 等备份文件
- 编辑器临时文件

### 测试和调试
- `test_output/`, `debug_logs/`
- `*.test.log`, `*.debug.log`

### 个人文件
- `.personal/`, `personal_notes/`, `scratch/`

### 数据库文件
- `*.db`, `*.sqlite`, `*.sqlite3`

## ✅ 应该提交的文件

- `README.md` - 项目说明
- `CHANGELOG.md` - 正式变更日志
- `app/src/` - 所有源代码
- `*.gradle.kts` - Gradle构建文件
- `app/src/main/res/` - 资源文件

## ⚠️ 注意事项

1. **已跟踪的文件**: `.gitignore` 只影响未跟踪的文件
2. **敏感信息**: 密钥、密码等绝对不能提交
3. **团队协作**: 确保团队成员使用相同的 `.gitignore`

## 🔧 故障排除

### 文件仍然被跟踪？
```bash
# 先移除缓存
git rm --cached filename

# 然后提交
git commit -m "Remove file from tracking"
```

### 想要强制添加被忽略的文件？
```bash
git add -f filename
```

### 清理所有未跟踪的文件？
```bash
# 预览
git clean -ndf

# 执行
git clean -fdf
```
